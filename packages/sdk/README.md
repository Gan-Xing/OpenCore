# @opencore/sdk

S6 SDK baseline for Auth and RBAC APIs.

Current scope:

- typed login and current-user contracts;
- typed RBAC list contracts for users, roles, permissions, and menus;
- a small request-function based client used by Admin pages;
- registry trace tests so SDK permission/menu types stay aligned with module registry.
