# @opencore/shared

S3 shared helpers used by contracts and module registry packages.

Current scope:

- runtime validation result helpers;
- small type guards used by schema validators;
- no application state, database access, auth, or business CRUD.
