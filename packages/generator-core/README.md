# @opencore/generator-core

OpenForge generator core owns schema/config loading, registry and OpenAPI
readers, validation, template rendering, VFS diffing, safe apply, rollback, and
doctor checks.

`tools/generator` remains the CLI wrapper for `pnpm openforge:*` commands.
