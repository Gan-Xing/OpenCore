# OpenCore Cycle-022 Acceptance Matrix

Date: 2026-06-23

| Area          | Requirement                                                          | Status     | Evidence                                                                                                                                             |
| ------------- | -------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Identity      | Global User can have tenant memberships                              | Done (T1)  | `TenantMembership` model and root backfill added. Multi-tenant login remains T2.                                                                     |
| Identity      | Single-tenant mode keeps default root tenant                         | Done (T1)  | `tenant_root` migration/seed and `OPENCORE_TENANCY_MODE` summary default to `single`.                                                                |
| Identity      | Token/session bound to tenant and membership                         | Done (T2)  | Token payload includes `tid/mid/am`; `OnlineUserSession` stores `tenantId`, `membershipId`, and `accessMode`.                                        |
| Identity      | Select/switch tenant reissues token                                  | Done (T2)  | `/api/auth/select-tenant` exchanges a login ticket; `/api/auth/switch-tenant` reissues a token and revokes the old session.                          |
| Platform auth | Platform admin model separated from tenant roles                     | Done (T1)  | `PlatformRole`, `UserPlatformRole`, `PlatformRolePermission`, `platform-admin` seed added. Platform visit runtime remains T6.                        |
| Plans         | Tenant plan uses module codes                                        | Done (T1)  | `TenantPlanModule.moduleCode`; root `system.full` seed syncs `listModules()`.                                                                        |
| Plans         | Tenant role permissions clipped by enabled plan modules              | Done (T3a) | Prisma RBAC repository derives permissions from active membership roles and filters registry permissions by `TenantPlanModule`.                      |
| Database      | Root tenant backfills existing users                                 | Done (T1)  | Migration and seed create root memberships for all users. Smoke checks parity.                                                                       |
| Database      | Legacy UserRole/UserPost copied to membership bridges                | Done (T1)  | `TenantMembershipRole` and `TenantMembershipPost` migration/seed added.                                                                              |
| Database      | Auth reads membership role/post bridges                              | Done (T3a) | Authenticated users now expose membership `roleCodes`, `postCodes`, and plan-clipped `permissionCodes`.                                              |
| Database      | Role catalog scoped by active tenant                                 | Done (T3b) | `Role.tenantId`, `(tenantId, code)` uniqueness, `PrismaSystemRoleRepository` request-context scoping, and root legacy bridge update.                 |
| Database      | Post catalog scoped by active tenant                                 | Done (T3c) | `SystemPost.tenantId`, `(tenantId, code)` uniqueness, request-context scoping, and root legacy bridge update.                                        |
| Database      | Department catalog scoped by active tenant                           | Done (T3d) | `SystemDept.tenantId`, `(tenantId, code)` uniqueness, request-context scoping, same-tenant parent/member-dept checks, and root legacy bridge update. |
| Database      | Tenant member assignments scoped by active tenant                    | Done (T3e) | `TenantFoundationService.updateMemberAssignments()` validates membership/dept/role/post in `RequestContext.tenantId` and syncs root legacy bridges.  |
| Database      | Online sessions scoped by active tenant                              | Done (T4a) | `PrismaOnlineUserRepository` scopes monitor list/detail/summary/cleanup/kick-out by `RequestContext.tenantId`; auth token lookup stays token-scoped. |
| Database      | Login logs scoped by active tenant                                   | Done (T4b) | `LoginLog.tenantId` plus repository `RequestContext.tenantId` scoping cover list/detail/export/delete/clean and record creation.                     |
| Database      | Operation audit logs scoped by active tenant                         | Done (T4c) | `AuditLog.tenantId` plus repository `RequestContext.tenantId` scoping cover list/detail/export/delete/retention clean and record creation.           |
| Database      | Tenant-owned repositories scoped by tenant                           | Partial    | Role, Post, Department, active member assignment, online sessions, Login logs, and Operation audit logs are done. Broader System/core repositories remain T4. |
| Database      | Composite unique constraints for tenant-owned codes                  | Partial    | Role, Post, and Department `(tenantId, code)` are done. Dict/Config uniqueness remains pending.                                                      |
| API           | Ordinary API does not trust body/query/header tenantId               | Done (T2)  | Guards populate tenant context from validated bearer token/session; smoke proves forged `tenant-id` header does not change context.                  |
| API           | Data scope resolves from active tenant membership                    | Done (T3a) | `SecurityDataScopeService` passes active membership id to the RBAC repository.                                                                       |
| API           | Active tenant member assignments exposed                             | Done (T3e) | `GET /api/core/tenancy/members` and `PATCH /api/core/tenancy/members/:membershipId/assignments`; no trusted client tenant selector.                  |
| OpenAPI       | Tenant foundation/auth/member/session/log DTOs exported              | Done (T4c) | Login-log and operation-audit DTOs expose `tenantId`; OpenAPI snapshot was refreshed.                                                                |
| SDK           | Tenant foundation/auth/member/session/log callable through SDK       | Done (T4c) | SDK login-log and audit-log summaries/fixtures include `tenantId` beside existing operations client calls.                                           |
| Admin         | Tenant foundation, members, session/log tenant fields visible        | Done (T4c) | `/system/tenants` remains live; `/monitor/online-users`, `/security/login-logs`, and `/security/operation-logs` expose tenant fields from live data. |
| Smoke         | Tenant foundation/auth/RBAC/org/member/session/log smokes exist      | Done (T4c) | `smoke:core-audit-log` seeds a foreign tenant audit log and proves root-scope list/detail/delete/clean do not cross tenants.                         |
| Guard         | Tenant foundation/auth/RBAC/org/member/session/log guards exist      | Done (T4c) | Adds `pnpm guard:tenant-operation-log-scope` to the existing tenant guard set.                                                                       |
| Runtime       | Redis/file/queue/WebSocket/Integration tenant propagation            | Pending    | T5.                                                                                                                                                  |
| Audit         | Platform visit audit                                                 | Pending    | T6. Login-log and operation-audit tenant ownership are done; platform visit audit remains.                                                           |
| Delivery      | lint/typecheck/test/smoke/deploy pass                                | Done (T4c) | Full lint/typecheck/test, deploy, local deploy smoke, public Admin checks, and public API operation-audit smoke passed.                            |
