/goal

Status: archived.

This prompt started the original quality-recursion run that produced cycles
001-020. It is no longer an execution entrypoint. Current work must use
`docs/quality-cycle/cycle-021/handoff.md` plus the active thread goal.

Historical rules worth keeping:

- A cycle only counted after audit, backlog completion, gate evidence and a
  ledger marker.
- Reference comparison was capability-based across NestWeb, Antdpro6, RuoYi
  and Yudao.
- CRM, ERP, MES, WMS, mall, payment, member, tenant, AI, RAG and Agent work
  required explicit admission.
- Failed checks were blockers; deleting security constraints to pass checks was
  forbidden.

Do not paste standard test, build, deploy or smoke command lists into quality
cycle documents. Durable state belongs in the ledger, handoff, backlog,
implementation notes and current completion response.
